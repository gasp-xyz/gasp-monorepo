apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.name }}
spec:
  selector:
  {{- toYaml .Values.serviceSelector | nindent 4 }}
  ports:
    - name: rpc
      protocol: TCP
      port: {{ .Values.port }}
      targetPort: {{ .Values.port }}