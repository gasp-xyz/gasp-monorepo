# rust:
#   syncToolchainConfig: false
#   version: 1.82.0
#   targets: [ "wasm32-unknown-unknown" ]

node:
  # Disabled so that moon doesn't explicitely add a node constraint to package.json
  addEnginesConstraint: false
  packageManager: yarn
  yarn:
    version: 4.7.0
